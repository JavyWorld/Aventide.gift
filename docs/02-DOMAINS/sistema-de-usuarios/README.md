# Alias legacy: `sistema-de-usuarios`

> ⚠️ Este directorio se mantiene solo por compatibilidad de enlaces históricos.

Este dominio fue normalizado al dominio canónico [`users`](../users/).

- Ruta canónica: `docs/02-DOMAINS/users/`
- Acción requerida: actualiza cualquier referencia de `sistema-de-usuarios` a `users`.

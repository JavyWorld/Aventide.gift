# Alias legacy: `sistema-de-contenido`

> ⚠️ Este directorio se mantiene solo por compatibilidad de enlaces históricos.

Este dominio fue normalizado al dominio canónico [`content`](../content/).

- Ruta canónica: `docs/02-DOMAINS/content/`
- Acción requerida: actualiza cualquier referencia de `sistema-de-contenido` a `content`.

# Alias legacy: `sistema-de-moderacion`

> ⚠️ Este directorio se mantiene solo por compatibilidad de enlaces históricos.

Este dominio fue normalizado al dominio canónico [`moderation`](../moderation/).

- Ruta canónica: `docs/02-DOMAINS/moderation/`
- Acción requerida: actualiza cualquier referencia de `sistema-de-moderacion` a `moderation`.

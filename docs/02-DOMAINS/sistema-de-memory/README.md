# Alias legacy: `sistema-de-memory`

> ⚠️ Este directorio se mantiene solo por compatibilidad de enlaces históricos.

Este dominio fue normalizado al dominio canónico [`memory`](../memory/).

- Ruta canónica: `docs/02-DOMAINS/memory/`
- Acción requerida: actualiza cualquier referencia de `sistema-de-memory` a `memory`.

openapi: 3.0.3
info:
  title: Sistema De Capacidad Disponibilidad API Contract
  version: v1.0.0
  description: Contrato mínimo del bounded context `sistema-de-capacidad-disponibilidad`.
servers:
  - url: https://api.aventide.gift
paths:
  /sistema-de-capacidad-disponibilidad/health:
    get:
      summary: Healthcheck del dominio sistema-de-capacidad-disponibilidad
      operationId: getSistemaDeCapacidadDisponibilidadHealth
      responses:
        '200':
          description: Dominio operativo
          content:
            application/json:
              schema:
                type: object
                required: [status, version]
                properties:
                  status:
                    type: string
                    enum: [ok]
                  version:
                    type: string
                    example: v1.0.0
  /sistema-de-capacidad-disponibilidad/commands:
    post:
      summary: Comando genérico del dominio sistema-de-capacidad-disponibilidad
      operationId: postSistemaDeCapacidadDisponibilidadCommand
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SistemaDeCapacidadDisponibilidadCommand'
      responses:
        '202':
          description: Comando aceptado
components:
  schemas:
    SistemaDeCapacidadDisponibilidadCommand:
      type: object
      additionalProperties: false
      required: [id, type, timestamp]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          example: sistema-de-capacidad-disponibilidad.execute
        timestamp:
          type: string
          format: date-time
        payload:
          type: object

openapi: 3.0.3
info:
  title: Sistema De Barrio API Contract
  version: v1.0.0
  description: Contrato mínimo del bounded context `sistema-de-barrio`.
servers:
  - url: https://api.aventide.gift
paths:
  /sistema-de-barrio/health:
    get:
      summary: Healthcheck del dominio sistema-de-barrio
      operationId: getSistemaDeBarrioHealth
      responses:
        '200':
          description: Dominio operativo
          content:
            application/json:
              schema:
                type: object
                required: [status, version]
                properties:
                  status:
                    type: string
                    enum: [ok]
                  version:
                    type: string
                    example: v1.0.0
  /sistema-de-barrio/commands:
    post:
      summary: Comando genérico del dominio sistema-de-barrio
      operationId: postSistemaDeBarrioCommand
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SistemaDeBarrioCommand'
      responses:
        '202':
          description: Comando aceptado
components:
  schemas:
    SistemaDeBarrioCommand:
      type: object
      additionalProperties: false
      required: [id, type, timestamp]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          example: sistema-de-barrio.execute
        timestamp:
          type: string
          format: date-time
        payload:
          type: object

openapi: 3.0.3
info:
  title: Sistema De Continuidad V2 API Contract
  version: v1.0.0
  description: Contrato mínimo del bounded context `sistema-de-continuidad-v2`.
servers:
  - url: https://api.aventide.gift
paths:
  /sistema-de-continuidad-v2/health:
    get:
      summary: Healthcheck del dominio sistema-de-continuidad-v2
      operationId: getSistemaDeContinuidadV2Health
      responses:
        '200':
          description: Dominio operativo
          content:
            application/json:
              schema:
                type: object
                required: [status, version]
                properties:
                  status:
                    type: string
                    enum: [ok]
                  version:
                    type: string
                    example: v1.0.0
  /sistema-de-continuidad-v2/commands:
    post:
      summary: Comando genérico del dominio sistema-de-continuidad-v2
      operationId: postSistemaDeContinuidadV2Command
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SistemaDeContinuidadV2Command'
      responses:
        '202':
          description: Comando aceptado
components:
  schemas:
    SistemaDeContinuidadV2Command:
      type: object
      additionalProperties: false
      required: [id, type, timestamp]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          example: sistema-de-continuidad-v2.execute
        timestamp:
          type: string
          format: date-time
        payload:
          type: object

openapi: 3.0.3
info:
  title: Sistema De Reputacion Buyer API Contract
  version: v1.0.0
  description: Contrato mínimo del bounded context `sistema-de-reputacion-buyer`.
servers:
  - url: https://api.aventide.gift
paths:
  /sistema-de-reputacion-buyer/health:
    get:
      summary: Healthcheck del dominio sistema-de-reputacion-buyer
      operationId: getSistemaDeReputacionBuyerHealth
      responses:
        '200':
          description: Dominio operativo
          content:
            application/json:
              schema:
                type: object
                required: [status, version]
                properties:
                  status:
                    type: string
                    enum: [ok]
                  version:
                    type: string
                    example: v1.0.0
  /sistema-de-reputacion-buyer/commands:
    post:
      summary: Comando genérico del dominio sistema-de-reputacion-buyer
      operationId: postSistemaDeReputacionBuyerCommand
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SistemaDeReputacionBuyerCommand'
      responses:
        '202':
          description: Comando aceptado
components:
  schemas:
    SistemaDeReputacionBuyerCommand:
      type: object
      additionalProperties: false
      required: [id, type, timestamp]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          example: sistema-de-reputacion-buyer.execute
        timestamp:
          type: string
          format: date-time
        payload:
          type: object

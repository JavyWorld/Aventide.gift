openapi: 3.0.3
info:
  title: Sistema De Copia De Seguridad API Contract
  version: v1.0.0
  description: Contrato mínimo del bounded context `sistema-de-copia-de-seguridad`.
servers:
  - url: https://api.aventide.gift
paths:
  /sistema-de-copia-de-seguridad/health:
    get:
      summary: Healthcheck del dominio sistema-de-copia-de-seguridad
      operationId: getSistemaDeCopiaDeSeguridadHealth
      responses:
        '200':
          description: Dominio operativo
          content:
            application/json:
              schema:
                type: object
                required: [status, version]
                properties:
                  status:
                    type: string
                    enum: [ok]
                  version:
                    type: string
                    example: v1.0.0
  /sistema-de-copia-de-seguridad/commands:
    post:
      summary: Comando genérico del dominio sistema-de-copia-de-seguridad
      operationId: postSistemaDeCopiaDeSeguridadCommand
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SistemaDeCopiaDeSeguridadCommand'
      responses:
        '202':
          description: Comando aceptado
components:
  schemas:
    SistemaDeCopiaDeSeguridadCommand:
      type: object
      additionalProperties: false
      required: [id, type, timestamp]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          example: sistema-de-copia-de-seguridad.execute
        timestamp:
          type: string
          format: date-time
        payload:
          type: object

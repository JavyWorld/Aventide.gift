openapi: 3.0.3
info:
  title: Sistema De Credito Interno API Contract
  version: v1.0.0
  description: Contrato mínimo del bounded context `sistema-de-credito-interno`.
servers:
  - url: https://api.aventide.gift
paths:
  /sistema-de-credito-interno/health:
    get:
      summary: Healthcheck del dominio sistema-de-credito-interno
      operationId: getSistemaDeCreditoInternoHealth
      responses:
        '200':
          description: Dominio operativo
          content:
            application/json:
              schema:
                type: object
                required: [status, version]
                properties:
                  status:
                    type: string
                    enum: [ok]
                  version:
                    type: string
                    example: v1.0.0
  /sistema-de-credito-interno/commands:
    post:
      summary: Comando genérico del dominio sistema-de-credito-interno
      operationId: postSistemaDeCreditoInternoCommand
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SistemaDeCreditoInternoCommand'
      responses:
        '202':
          description: Comando aceptado
components:
  schemas:
    SistemaDeCreditoInternoCommand:
      type: object
      additionalProperties: false
      required: [id, type, timestamp]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          example: sistema-de-credito-interno.execute
        timestamp:
          type: string
          format: date-time
        payload:
          type: object

openapi: 3.0.3
info:
  title: Sistema De Auditoria API Contract
  version: v1.0.0
  description: Contrato mínimo del bounded context `sistema-de-auditoria`.
servers:
  - url: https://api.aventide.gift
paths:
  /sistema-de-auditoria/health:
    get:
      summary: Healthcheck del dominio sistema-de-auditoria
      operationId: getSistemaDeAuditoriaHealth
      responses:
        '200':
          description: Dominio operativo
          content:
            application/json:
              schema:
                type: object
                required: [status, version]
                properties:
                  status:
                    type: string
                    enum: [ok]
                  version:
                    type: string
                    example: v1.0.0
  /sistema-de-auditoria/commands:
    post:
      summary: Comando genérico del dominio sistema-de-auditoria
      operationId: postSistemaDeAuditoriaCommand
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SistemaDeAuditoriaCommand'
      responses:
        '202':
          description: Comando aceptado
components:
  schemas:
    SistemaDeAuditoriaCommand:
      type: object
      additionalProperties: false
      required: [id, type, timestamp]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          example: sistema-de-auditoria.execute
        timestamp:
          type: string
          format: date-time
        payload:
          type: object

openapi: 3.0.3
info:
  title: Sistema De Reputacion Seller API Contract
  version: v1.0.0
  description: Contrato mínimo del bounded context `sistema-de-reputacion-seller`.
servers:
  - url: https://api.aventide.gift
paths:
  /sistema-de-reputacion-seller/health:
    get:
      summary: Healthcheck del dominio sistema-de-reputacion-seller
      operationId: getSistemaDeReputacionSellerHealth
      responses:
        '200':
          description: Dominio operativo
          content:
            application/json:
              schema:
                type: object
                required: [status, version]
                properties:
                  status:
                    type: string
                    enum: [ok]
                  version:
                    type: string
                    example: v1.0.0
  /sistema-de-reputacion-seller/commands:
    post:
      summary: Comando genérico del dominio sistema-de-reputacion-seller
      operationId: postSistemaDeReputacionSellerCommand
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SistemaDeReputacionSellerCommand'
      responses:
        '202':
          description: Comando aceptado
components:
  schemas:
    SistemaDeReputacionSellerCommand:
      type: object
      additionalProperties: false
      required: [id, type, timestamp]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          example: sistema-de-reputacion-seller.execute
        timestamp:
          type: string
          format: date-time
        payload:
          type: object
